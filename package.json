{
  "name": "crud-mongo",
  "version": "0.0.0",
  "private": true,
  "main": "./bin/www",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -L main",
    "docker-down-all":"docker-compose down --rmi all; docker rmi -f $(docker images -aq); docker volume rm $(docker volume ls -q)"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^16.0.0",
    "express": "~4.16.1",
    "fastest-validator": "^1.12.0",
    "mongoose": "^6.2.3",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.15"
  }
}
